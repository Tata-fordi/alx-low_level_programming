#include "main.h"
#include <stdio.h>
/**
  *mul - multiplying two numbers
  *@a: first integer
  *@b: second integer
  *Return: value of the multiplication
  */
int mul(int a, int b)
{
	return (a * b);
}
